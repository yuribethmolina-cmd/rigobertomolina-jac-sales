
## Plan: Cambiar fuentes a Barlow Condensed + DM Sans

### Cambios

1. **`src/index.css`** — Reemplazar el import de Google Fonts de `Chakra Petch + Outfit` por `Barlow Condensed` (wght 400-800) y `DM Sans` (wght 300-700).

2. **`tailwind.config.ts`** — Actualizar `fontFamily`:
   - `heading`: `"Barlow Condensed"` 
   - `body`: `"DM Sans"`

Dos archivos, cambio mínimo.
