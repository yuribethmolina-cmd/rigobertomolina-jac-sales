// Release identifier shared between client code (footer, logs) and
// build-time HTML transforms (favicon cache-busting).
// Bump this on each meaningful release.
export const RELEASE_ID = "v20260819-1200";
